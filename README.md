<h1>Selenium Practice</h1>

Repository created for the practice of web activities, using the Selenium framework, along with Java.
Practice is done using web pages found on the internet, mainly to improve skills in creating classes in Java.

The repository is divided into folders to recognize the classes and uses a central folder for proper control of the classes to search for the elements.
